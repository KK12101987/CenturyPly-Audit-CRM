services:
  - type: web
    name: centuryply-qa-audit
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn centuryply_audit_webapp:app -b 0.0.0.0:8080
